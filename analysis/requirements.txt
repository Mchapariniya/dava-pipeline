# Requirements for the DAVA analysis package (NER / sentiment / emotion /
# topic modelling / visualisation).
#
# The package is designed to degrade gracefully: with only the "always"
# dependencies below it runs fully offline (spaCy NER + lexicon sentiment/
# emotion + scikit-learn LDA/NMF topics + matplotlib/wordcloud diagrams).
# Install the "best quality" extras to switch the transformer-based backends on.

# ----- always needed (offline-capable core) -----
scikit-learn>=1.3          # LDA / NMF topic modelling, TF-IDF
matplotlib>=3.7            # charts
wordcloud>=1.9             # word clouds
pandas>=1.5               # tabular handling
numpy>=1.24
langdetect>=1.0.9          # language fallback when segment codes are missing

# spaCy multilingual NER (offline). After installing, download a model, e.g.:
#   python -m spacy download xx_ent_wiki_sm      # multilingual (small)
#   python -m spacy download de_core_news_sm     # German
spacy>=3.7

# ----- best quality (optional; enable transformer backends) -----
# transformers>=4.30
# torch>=2.0
# sentencepiece                # needed by many multilingual tokenisers

# ----- best quality topic modelling (optional) -----
# bertopic>=0.16
# sentence-transformers>=2.2
