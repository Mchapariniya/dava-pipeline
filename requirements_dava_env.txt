# =============================================================================
# requirements_dava_env.txt
# conda env:  dava_env
# Purpose:    Stage 3-6 — analysis (NER / sentiment / emotion / topics),
#             the DAVA Insights dashboard, ELAN export, and RAG (retrieval +
#             local Ollama generation).
#
#   conda create -n dava_env python=3.10 -y
#   conda activate dava_env
#   pip install -r requirements_dava_env.txt
#   python -m spacy download xx_ent_wiki_sm     # multilingual NER model
#
# The Ollama *server* is installed separately (see ANALYSIS.md, "Ollama").
# This env only needs the `requests` client to talk to it.
# =============================================================================

# --- Dashboard -----------------------------------------------------------------
streamlit>=1.28,<2
plotly>=5.15
pandas>=1.5
numpy>=1.24

# --- Analysis: offline-capable core -------------------------------------------
scikit-learn>=1.3                 # LDA / NMF topic modelling, TF-IDF
matplotlib>=3.7                   # charts
wordcloud>=1.9                    # word clouds
langdetect>=1.0.9                 # language fallback + RAG language routing
spacy>=3.7                        # multilingual NER (then download a model, above)

# --- RAG: semantic retrieval ---------------------------------------------------
# sentence-transformers pulls in torch + transformers, which ALSO switches on the
# best-quality (transformer) NER / sentiment / emotion backends automatically.
sentence-transformers>=2.2
requests>=2.28                    # talks to the local Ollama server

# --- Optional: best-quality topic modelling -----------------------------------
# bertopic>=0.16

# --- Optional: persistent, multi-document RAG index ----------------------------
# (the dashboard uses a fast in-memory index; ChromaDB is only needed if you
#  want an index that persists across sessions / many files)
# chromadb>=0.4

# --- Optional: stronger cross-lingual embeddings (larger download) -------------
# these are model choices, not packages — set in the Ask tab's "Advanced" panel:
#   intfloat/multilingual-e5-large   or   BAAI/bge-m3
